# AGENTS.md

## 项目说明

本工作区用于开发 Codex skill，所有说明和交付内容优先使用中文。

## PowerShell 编码说明

- 终端看到中文乱码时，不要立刻判断文件损坏，先当作显示或回传异常处理。
- 读取中文源码时，先单独执行 `Get-Content -LiteralPath <path>`，不要一开始并行读多个文件。
- 如结果异常，再用 `[System.IO.File]::ReadAllText((Resolve-Path <path>), [System.Text.Encoding]::UTF8)` 复核。
- 修改任何包含中文文本文件时，读写显式使用 UTF-8，勿依赖默认编码。

## 编程行为规范

- 编码前先明确假设、权衡和不确定点。
- 简洁优先，只实现需求需要的最少内容。
- 精准修改，只动与任务直接相关的文件。
- 目标驱动执行，把任务转化为可验证目标并循环到验证通过。
- 风险操作默认保守，不主动做大范围重构、批量删除、依赖升级或发布相关变更。
